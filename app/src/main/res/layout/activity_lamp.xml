<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fonView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.Lamp"
    tools:visibility="visible">

    <ImageView
        android:id="@+id/imageViewButtonOff"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:srcCompat="@drawable/button_off"
        android:contentDescription="@string/button_of" />

    <TextView
        android:id="@+id/textViewButtonOff"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/turn_off"
        android:textColor="@color/white"
        android:textSize="12sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@+id/imageViewButtonOff"
        app:layout_constraintEnd_toEndOf="@+id/imageViewButtonOff"
        app:layout_constraintStart_toStartOf="@+id/imageViewButtonOff"
        app:layout_constraintTop_toTopOf="@+id/imageViewButtonOff" />
</androidx.constraintlayout.widget.ConstraintLayout>